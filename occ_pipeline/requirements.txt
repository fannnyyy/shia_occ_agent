# Étape 1 — Génération du dataset
requests>=2.31.0

# Étape 2 — Fine-tuning LoRA
torch>=2.1.0
transformers>=4.38.0
peft>=0.9.0
trl>=0.8.0
datasets>=2.17.0
accelerate>=0.27.0
bitsandbytes>=0.42.0  # quantisation 4-bit (GPU uniquement, pas requis sur CPU)

# Étape 3 — Serveur HTTP
flask>=3.0.0
